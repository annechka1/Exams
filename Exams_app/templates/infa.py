"""import random

def make_information(inf_id,price):
    raise Exception("Secret information")

def buy(count,product_id,inf_id):
    prices={}
    for id in range(100):
        prices[id] = random.randint(10, 100)
    product_price = prices[product_id]
    volume_of_sales=count*product_price
    if make_information(inf_id,product_price):
        return "Ok"
    else:
        return "Error"""""



